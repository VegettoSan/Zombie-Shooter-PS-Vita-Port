diff --git a/FalsoNDK_Utils.cpp b/FalsoNDK_Utils.cpp
index b600e3d..5b30221 100644
--- a/FalsoNDK_Utils.cpp
+++ b/FalsoNDK_Utils.cpp
@@ -46,2 +46,3 @@ void LOG_ALWAYS_FATAL(const char * fmt, ...) {
     sceClibAbort();
+    __builtin_unreachable();
 }
@@ -53,3 +54,5 @@ void ALOGE(const char * fmt, ...) {
 void ALOGW(const char * fmt, ...) {
+#ifndef ZOMBIE_RELEASE_BUILD
     LOG_PRINT(FALSONDK_LOG_WARN, fmt);
+#endif
 }
@@ -57,3 +60,5 @@ void ALOGW(const char * fmt, ...) {
 void ALOGD(const char * fmt, ...) {
+#ifndef ZOMBIE_RELEASE_BUILD
     LOG_PRINT(FALSONDK_LOG_DEBUG, fmt);
+#endif
 }
diff --git a/android/AAssetManager.cpp b/android/AAssetManager.cpp
index 3e61b41..4cab4e1 100644
--- a/android/AAssetManager.cpp
+++ b/android/AAssetManager.cpp
@@ -3,2 +3,3 @@
 #include <psp2/kernel/threadmgr.h>
+#include <psp2/io/stat.h>
 #include <malloc.h>
@@ -8,2 +9,3 @@
 #include <fcntl.h>
+#include <cerrno>
 
@@ -23,4 +25,8 @@ typedef struct aAsset {
     FILE* f;
+    unsigned char *buffer;
+    size_t position;
     size_t bytesRead;
     size_t fileSize;
+    unsigned diagnosticReadCalls;
+    bool traceFootsteps;
     bool opened = false;
@@ -51,3 +57,7 @@ AAsset* AAssetManager_open(AAssetManager* mgr, const char* filename, int mode) {
     strcpy(a->filename, realp.c_str());
+    a->buffer = nullptr;
+    a->position = 0;
     a->bytesRead = 0;
+    a->diagnosticReadCalls = 0;
+    a->traceFootsteps = realp.find("wav/footsteps.wav") != std::string::npos;
 
@@ -60,2 +70,13 @@ AAsset* AAssetManager_open(AAssetManager* mgr, const char* filename, int mode) {
     if (!a->f) {
+        int open_errno = errno;
+        if (!strstr(filename, "i18n/") && !strstr(filename, "i18n\\")) {
+            static unsigned failure_count = 0;
+            unsigned count = __sync_add_and_fetch(&failure_count, 1);
+            if (count <= 4 || (count & (count - 1)) == 0) {
+                SceIoStat stat;
+                int stat_result = sceIoGetstat(realp.c_str(), &stat);
+                ALOGW("[ASSET] open failed #%u: %s errno=%d stat=0x%x",
+                      count, realp.c_str(), open_errno, stat_result);
+            }
+        }
         free(a->filename);
@@ -74,2 +95,41 @@ AAsset* AAssetManager_open(AAssetManager* mgr, const char* filename, int mode) {
         a->opened = true;
+
+        // The engine keeps many assets open at once. Vita's stdio file slots
+        // run out first on .vid files and later on menu images and definitions.
+        // Keep small read-only assets in memory and release their FILE handles.
+        // Larger non-.vid files stay streamed to bound memory use.
+        const char *extension = strrchr(a->filename, '.');
+        bool is_vid = extension && strcmp(extension, ".vid") == 0;
+        if (a->fileSize <= 256 * 1024 || (is_vid && a->fileSize <= 2 * 1024 * 1024)) {
+            unsigned char *buffer = (unsigned char *)malloc(a->fileSize ? a->fileSize : 1);
+            if (buffer) {
+#ifdef USE_SCELIBC_IO
+                size_t loaded = sceLibcBridge_fread(buffer, 1, a->fileSize, a->f);
+#else
+                size_t loaded = fread(buffer, 1, a->fileSize, a->f);
+#endif
+                if (loaded == a->fileSize) {
+#ifdef USE_SCELIBC_IO
+                    sceLibcBridge_fclose(a->f);
+#else
+                    fclose(a->f);
+#endif
+                    a->f = nullptr;
+                    a->opened = false;
+                    a->buffer = buffer;
+                    static unsigned buffered_count = 0;
+                    unsigned count = __sync_add_and_fetch(&buffered_count, 1);
+                    if (count <= 4 || (count % 64) == 0)
+                        ALOGD("[ASSET] buffered #%u: %s (%u bytes)",
+                              count, a->filename, (unsigned)a->fileSize);
+                } else {
+                    free(buffer);
+#ifdef USE_SCELIBC_IO
+                    sceLibcBridge_fseek(a->f, 0, SEEK_SET);
+#else
+                    fseek(a->f, 0, SEEK_SET);
+#endif
+                }
+            }
+        }
     }
@@ -82,6 +142,7 @@ AAsset* AAssetManager_open(AAssetManager* mgr, const char* filename, int mode) {
 void AAsset_close(AAsset* asset) {
-    ALOGD("AAsset_close<%p>(%p)", __builtin_return_address(0), asset);
-
     if (asset) {
         auto * a = (aAsset *) asset;
+        ALOGD("AAsset_close<%p>(%p, %s, read=%u/%u)",
+              __builtin_return_address(0), asset, a->filename,
+              (unsigned)a->bytesRead, (unsigned)a->fileSize);
         free(a->filename);
@@ -94,2 +155,3 @@ void AAsset_close(AAsset* asset) {
         }
+        free(a->buffer);
         delete a;
@@ -99,5 +161,4 @@ void AAsset_close(AAsset* asset) {
 int AAsset_read(AAsset* asset, void* buf, size_t count) {
-    ALOGD("AAsset_read<%p>(%p, %p, %i)", __builtin_return_address(0), asset, buf, count);
-
     if (!asset) {
+        ALOGW("AAsset_read called with NULL asset");
         return -1;
@@ -107,3 +168,4 @@ int AAsset_read(AAsset* asset, void* buf, size_t count) {
 
-    if (!a->opened) {
+    if (!a->opened && !a->buffer) {
+        ALOGW("AAsset_read called on closed asset: %s", a->filename);
         return -1;
@@ -111,12 +173,43 @@ int AAsset_read(AAsset* asset, void* buf, size_t count) {
 
+    size_t ret;
+    if (a->buffer) {
+        size_t available = a->fileSize - a->position;
+        ret = count < available ? count : available;
+        if (ret) memcpy(buf, a->buffer + a->position, ret);
+        a->position += ret;
+    } else {
 #ifdef USE_SCELIBC_IO
-    size_t ret = sceLibcBridge_fread(buf, 1, count, a->f);
+        ret = sceLibcBridge_fread(buf, 1, count, a->f);
 #else
-    size_t ret = fread(buf, 1, count, a->f);
+        ret = fread(buf, 1, count, a->f);
 #endif
+    }
 
     if (ret > 0) {
+        size_t previous = a->bytesRead;
         a->bytesRead += ret;
+        // Tiny reads from the game's binary decoder are common. A line and
+        // synchronous disk flush for every byte can stall asset loading.
+        if ((previous >> 14) != (a->bytesRead >> 14)) {
+            ALOGD("AAsset_read progress %s: %u/%u bytes",
+                  a->filename, (unsigned)a->bytesRead,
+                  (unsigned)a->fileSize);
+        }
+        if (a->traceFootsteps) {
+            unsigned calls = ++a->diagnosticReadCalls;
+            if (calls <= 4 || (a->buffer && a->position == a->fileSize))
+                ALOGD("[ASSET] footsteps read call=%u request=%u returned=%u pos=%u/%u",
+                      calls, (unsigned)count, (unsigned)ret,
+                      (unsigned)a->position, (unsigned)a->fileSize);
+        }
         return (int) ret;
     } else {
+        if (a->traceFootsteps) {
+            unsigned calls = ++a->diagnosticReadCalls;
+            if (calls <= 4 || (calls & (calls - 1)) == 0)
+                ALOGD("[ASSET] footsteps EOF/error call=%u request=%u pos=%u/%u",
+                      calls, (unsigned)count, (unsigned)a->position,
+                      (unsigned)a->fileSize);
+        }
+        if (a->buffer) return 0;
 #ifdef USE_SCELIBC_IO
@@ -128,2 +221,5 @@ int AAsset_read(AAsset* asset, void* buf, size_t count) {
         } else {
+            ALOGW("AAsset_read failed: %s at %u/%u bytes",
+                  a->filename, (unsigned)a->bytesRead,
+                  (unsigned)a->fileSize);
             return -1;
@@ -142,2 +238,12 @@ off_t AAsset_seek(AAsset* asset, off_t offset, int whence) {
 
+    if (a->buffer) {
+        int64_t base = whence == SEEK_SET ? 0 :
+                       whence == SEEK_CUR ? (int64_t)a->position :
+                       whence == SEEK_END ? (int64_t)a->fileSize : -1;
+        int64_t next = base + offset;
+        if (base < 0 || next < 0 || next > (int64_t)a->fileSize) return -1;
+        a->position = (size_t)next;
+        return (off_t)next;
+    }
+
     if (!a->opened) {
@@ -171,2 +277,4 @@ off_t AAsset_getRemainingLength(AAsset* asset) {
 
+    if (a->buffer) return (off_t)(a->fileSize - a->position);
+
     if (!a->opened) {
@@ -216,2 +324,15 @@ int AAsset_openFileDescriptor(AAsset* asset, off_t* outStart, off_t* outLength)
     if (outLength) *outLength = a->fileSize;
+    // This port's OpenSL ES backend does not implement AndroidFD sources.
+    // The game requests .m4a music through this API, then cannot create its
+    // music player and never closes the returned descriptor. Avoid exhausting
+    // Vita's descriptor table; PCM effect playback uses buffer queues instead.
+    const char *extension = strrchr(a->filename, '.');
+    if (extension && strcmp(extension, ".m4a") == 0) {
+        static unsigned unsupported_count = 0;
+        unsigned count = __sync_add_and_fetch(&unsupported_count, 1);
+        if (count <= 3 || (count % 64) == 0)
+            ALOGW("[AUDIO] AndroidFD .m4a unsupported; descriptor not opened (%u): %s",
+                  count, a->filename);
+        return -1;
+    }
     if (a->opened) {
diff --git a/android/AInput.cpp b/android/AInput.cpp
index 91f151d..c1c23d3 100644
--- a/android/AInput.cpp
+++ b/android/AInput.cpp
@@ -5,2 +5,3 @@
 #include <psp2/kernel/threadmgr.h>
+#include <psp2/kernel/processmgr.h>
 #include <cstring>
@@ -19,2 +20,5 @@ static int _event_pool_top = 0;
 static SceKernelLwMutexWork _event_pool_lock;
+static volatile unsigned input_enqueued;
+static volatile unsigned input_dequeued;
+static volatile unsigned input_last_report_ms;
 
@@ -45,2 +49,4 @@ AInputQueue * AInputQueue_create() {
         ALOGE("eventfd creation for AInputQueue failed: %s\n", strerror(errno));
+        delete iq;
+        return nullptr;
     } else {
@@ -122,2 +128,3 @@ int32_t AInputQueue_getEvent(AInputQueue* queue, AInputEvent** outEvent) {
         q->mPendingEvents.pop_front();
+        __atomic_add_fetch(&input_dequeued, 1, __ATOMIC_RELAXED);
     }
@@ -160,2 +167,7 @@ void AInputQueue_enqueueEvent(AInputQueue* queue, AInputEvent* event) {
     auto * q = reinterpret_cast<inputQueue *>(queue);
+    const inputEvent *sample = reinterpret_cast<const inputEvent *>(event);
+    int sample_source = sample->source;
+    int sample_type = sample->type;
+    int sample_action = sample_type == AINPUT_EVENT_TYPE_MOTION
+        ? sample->motion_action : sample->action;
 
@@ -163,2 +175,4 @@ void AInputQueue_enqueueEvent(AInputQueue* queue, AInputEvent* event) {
     q->mPendingEvents.push_back(event);
+    unsigned pending = (unsigned)q->mPendingEvents.size();
+    unsigned queued = __atomic_add_fetch(&input_enqueued, 1, __ATOMIC_RELAXED);
     if (q->mPendingEvents.size() == 1) {
@@ -171,2 +185,12 @@ void AInputQueue_enqueueEvent(AInputQueue* queue, AInputEvent* event) {
     sceKernelUnlockLwMutex(&q->mLock, 1);
+
+    unsigned now_ms = (unsigned)(sceKernelGetProcessTimeWide() / 1000);
+    unsigned last_ms = __atomic_load_n(&input_last_report_ms, __ATOMIC_RELAXED);
+    if (now_ms - last_ms >= 5000 &&
+        __atomic_compare_exchange_n(&input_last_report_ms, &last_ms, now_ms,
+                                    false, __ATOMIC_RELAXED, __ATOMIC_RELAXED)) {
+        ALOGD("[INPUT] queue pending=%u enqueued=%u dequeued=%u source=0x%x type=%d action=%d",
+              pending, queued, __atomic_load_n(&input_dequeued, __ATOMIC_RELAXED),
+              sample_source, sample_type, sample_action);
+    }
 }
@@ -200,3 +224,3 @@ int32_t AInputEvent_getDeviceId(const AInputEvent* event) {
     auto * e = reinterpret_cast<const inputEvent *>(event);
-    return e->source; // in our case that's fine, hopefully. unless there are external pads connected?
+    return e->device_id;
 }
@@ -266,2 +290,8 @@ float AMotionEvent_getY(const AInputEvent* motion_event, size_t pointer_index) {
 
+int32_t AMotionEvent_getButtonState(const AInputEvent* motion_event) {
+    // Synthetic Vita motion events have no motion button bits. Pad buttons
+    // are delivered separately as AKeyEvent input.
+    return 0;
+}
+
 float AMotionEvent_getAxisValue(const AInputEvent* motion_event,
@@ -271,2 +301,11 @@ int32_t axis, size_t pointer_index) {
     if (pointer_index >= 10) pointer_index = 9;
+#ifdef ZOMBIE_DEBUG_BUILD
+    // One trace per axis, including unsupported RX/RY. No per-frame logging.
+    static uint64_t seen_axes;
+    if (axis >= 0 && axis < 64) {
+        uint64_t bit = UINT64_C(1) << axis;
+        if (!(__atomic_fetch_or(&seen_axes, bit, __ATOMIC_RELAXED) & bit))
+            ALOGD("[INPUT] axis requested=%d deviceId=%d source=0x%x", axis, e->device_id, e->source);
+    }
+#endif
     switch (axis) {
@@ -295,3 +334,3 @@ int32_t axis, size_t pointer_index) {
         default:
-            ALOGE("AMotionEvent_getAxisValue: unexpected axis %i", axis);
+            // Unsupported axes are neutral; the bounded Debug trace identifies them.
             return 0;
diff --git a/android/AInput.h b/android/AInput.h
index a4bb775..70975d1 100644
--- a/android/AInput.h
+++ b/android/AInput.h
@@ -597,3 +597,6 @@ typedef struct AInputEvent AInputEvent;
  */
+enum { FNDK_GAMEPAD_DEVICE_ID = 1, FNDK_TOUCH_DEVICE_ID = 2 };
+
 typedef struct inputEvent {
+    int device_id; // Stable identity, independent of event source.
     int source; // one of AINPUT_SOURCE_* enum
@@ -698,2 +701,5 @@ float AMotionEvent_getY(const AInputEvent *motion_event, size_t pointer_index);
 
+/** Get the state of motion event buttons. Controller buttons use key events here. */
+int32_t AMotionEvent_getButtonState(const AInputEvent* motion_event);
+
 /** Get the value of the request axis for the given pointer index. */
diff --git a/android/ANativeActivity.cpp b/android/ANativeActivity.cpp
index efabbdf..f339b04 100644
--- a/android/ANativeActivity.cpp
+++ b/android/ANativeActivity.cpp
@@ -1,2 +1,3 @@
 #include "android/ANativeActivity.h"
+#include "FalsoNDK_Utils.h"
 
@@ -11,3 +12,6 @@ void ANativeActivity_setWindowFlags(ANativeActivity* activity, uint32_t addFlags
 void ANativeActivity_finish(ANativeActivity* activity) {
-    free(activity);
+    // Android only asks the framework to finish here.  The NativeActivity and
+    // its callback table are framework-owned and remain valid through the
+    // subsequent pause/stop/destroy callbacks; freeing them here is a UAF.
+    ALOGW("ANativeActivity_finish(activity=%p): finish requested", activity);
 }
diff --git a/android/ANativeWindow.cpp b/android/ANativeWindow.cpp
index 417bd71..ecf7668 100644
--- a/android/ANativeWindow.cpp
+++ b/android/ANativeWindow.cpp
@@ -3,7 +3,10 @@
 #include <cstdlib>
-#include <cstring>
 #include <cstdio>
+#include <cerrno>
 
 typedef struct nativeWindow {
-    int dummy;
+    int32_t width;
+    int32_t height;
+    int32_t format;
+    int32_t refs;
 } nativeWindow;
@@ -11,8 +14,9 @@ typedef struct nativeWindow {
 ANativeWindow * ANativeWindow_create() {
-    nativeWindow win;
-
-    auto ret = (ANativeWindow *) malloc(sizeof(nativeWindow));
-    memcpy(ret, &win, sizeof(nativeWindow));
-
-    return ret;
+    auto *win = static_cast<nativeWindow *>(calloc(1, sizeof(nativeWindow)));
+    if (!win) return nullptr;
+    win->width = 960;
+    win->height = 544;
+    win->format = 1; // AHARDWAREBUFFER_FORMAT_R8G8B8A8_UNORM
+    win->refs = 1;
+    return reinterpret_cast<ANativeWindow *>(win);
 }
@@ -20,3 +24,4 @@ ANativeWindow * ANativeWindow_create() {
 int32_t ANativeWindow_getWidth(ANativeWindow* window) {
-    return 960;
+    if (!window) return -EINVAL;
+    return reinterpret_cast<nativeWindow *>(window)->width;
 }
@@ -24,3 +29,4 @@ int32_t ANativeWindow_getWidth(ANativeWindow* window) {
 int32_t ANativeWindow_getHeight(ANativeWindow* window) {
-    return 544;
+    if (!window) return -EINVAL;
+    return reinterpret_cast<nativeWindow *>(window)->height;
 }
@@ -28,3 +34,4 @@ int32_t ANativeWindow_getHeight(ANativeWindow* window) {
 int32_t ANativeWindow_getFormat(ANativeWindow* window) {
-    return 1; // AHARDWAREBUFFER_FORMAT_R8G8B8A8_UNORM
+    if (!window) return -EINVAL;
+    return reinterpret_cast<nativeWindow *>(window)->format;
 }
@@ -33,2 +40,7 @@ int32_t ANativeWindow_setBuffersGeometry(ANativeWindow* window,
                                          int32_t width, int32_t height, int32_t format) {
+    if (!window || ((width == 0) != (height == 0))) return -EINVAL;
+    auto *win = reinterpret_cast<nativeWindow *>(window);
+    win->width = width ? width : 960;
+    win->height = height ? height : 544;
+    win->format = format ? format : 1;
     return 0;
diff --git a/shim/fndk_controls.cpp b/shim/fndk_controls.cpp
index 9a5866a..3cd4231 100644
--- a/shim/fndk_controls.cpp
+++ b/shim/fndk_controls.cpp
@@ -19,2 +19,3 @@
 #include "android/AInput.h"
+#include "FalsoNDK_Utils.h"
 
@@ -24,2 +25,5 @@ extern "C" {
     int fndk_button_event_source __attribute__((weak)) = AINPUT_SOURCE_GAMEPAD;
+    uint32_t __attribute__((weak)) fndk_translate_pad_buttons(uint32_t buttons, uint32_t rear, bool handheld) {
+        return buttons | (handheld ? rear : 0);
+    }
 }
@@ -156,2 +160,3 @@ void pollTouch() {
 
+                ev.device_id = FNDK_TOUCH_DEVICE_ID;
                 ev.source = AINPUT_SOURCE_TOUCHSCREEN;
@@ -270,4 +275,6 @@ extern "C" {
             { SCE_CTRL_SELECT,    AKEYCODE_BUTTON_SELECT },
+            { SCE_CTRL_L3,        AKEYCODE_BUTTON_THUMBL },
+            { SCE_CTRL_R3,        AKEYCODE_BUTTON_THUMBR },
     };
-    int fndk_button_mapping_count __attribute__((weak)) = 14;
+    int fndk_button_mapping_count __attribute__((weak)) = sizeof(fndk_button_mapping) / sizeof(fndk_button_mapping[0]);
 }
@@ -277,9 +284,10 @@ float lx = 0, ly = 0, rx = 0, ry = 0, lastLx = 0, lastLy = 0, lastRx = 0, lastRy
 
-inputEvent stickInputEvent;
+inputEvent stickInputEvent{};
 int sticksDown = 0;
 float x_old = 0.0f, y_old = 0.0f, z_old = 0.0f, rz_old = 0.0f, hat_x_old = 0.0f, hat_y_old = 0.0f;
-bool ltPressed_old = false, rtPressed_old = false;
+float ltPressed_old = 0, rtPressed_old = 0;
 
-void sendJoyEvent(float x, float y, float z, float rz, float hat_x, float hat_y, bool ltPressed, bool rtPressed) {
+void sendJoyEvent(float x, float y, float z, float rz, float hat_x, float hat_y, float ltPressed, float rtPressed) {
     if (x != x_old || y != y_old || z != z_old || rz != rz_old || hat_x != hat_x_old || hat_y != hat_y_old || ltPressed != ltPressed_old || rtPressed != rtPressed_old) {
+        stickInputEvent.device_id = FNDK_GAMEPAD_DEVICE_ID;
         stickInputEvent.source = AINPUT_SOURCE_JOYSTICK;
@@ -292,4 +300,4 @@ void sendJoyEvent(float x, float y, float z, float rz, float hat_x, float hat_y,
         stickInputEvent.motion_hat_y[0] = hat_y;
-        stickInputEvent.motion_lt[0] = ltPressed ? 1.0 : 0.0;
-        stickInputEvent.motion_rt[0] = rtPressed ? 1.0 : 0.0;
+        stickInputEvent.motion_lt[0] = ltPressed;
+        stickInputEvent.motion_rt[0] = rtPressed;
         stickInputEvent.motion_ptridx[0] = 0;
@@ -313,18 +321,37 @@ void sendJoyEvent(float x, float y, float z, float rz, float hat_x, float hat_y,
 void pollPad() {
-    SceCtrlData pad;
-    sceCtrlPeekBufferPositiveExt2(0, &pad, 1);
-
-    old_buttons = current_buttons;
-    current_buttons = pad.buttons;
-
-    if (!(current_buttons & SCE_CTRL_L2  || current_buttons & SCE_CTRL_R2)) {
-        SceTouchData t;
-        sceTouchPeek(SCE_TOUCH_PORT_BACK, &t, 1);
-
-        for (int i = 0; i < t.reportNum; i++) {
-            if (t.report[i].y < (panelInfoBack.minAaY + panelInfoBack.maxAaY) / 2) {
-                if (t.report[i].x < (panelInfoBack.minAaX + panelInfoBack.maxAaX) / 2) {
-                    if (t.report[i].x >= BACK_TOUCH_MARGIN) current_buttons |= SCE_CTRL_L2;
-                } else {
-                    if (t.report[i].x < (panelInfoBack.maxAaX - BACK_TOUCH_MARGIN)) current_buttons |= SCE_CTRL_R2;
+    // Check pairing once a second; external controllers retain natural bindings.
+    static unsigned port_check;
+    static int pad_port;
+    static bool handheld;
+    if (port_check++ % 60 == 0) {
+        SceCtrlPortInfo info{};
+        pad_port = 0;
+        handheld = false;
+        if (sceCtrlGetControllerPortInfo(&info) >= 0) {
+            handheld = info.port[0] == SCE_CTRL_TYPE_PHY;
+            for (int i = 0; i < 5; ++i) {
+                if (info.port[i] == SCE_CTRL_TYPE_DS3 || info.port[i] == SCE_CTRL_TYPE_DS4) {
+                    pad_port = i;
+                    handheld = false;
+                    break;
+                }
+            }
+        }
+    }
+    SceCtrlData pad{};
+    if (sceCtrlPeekBufferPositiveExt2(pad_port, &pad, 1) <= 0) {
+        // Release stale input on disconnect/read failure; keep centered sticks.
+        pad.lx = pad.ly = pad.rx = pad.ry = 128;
+    }
+    uint32_t rear_buttons = 0;
+    if (handheld) {
+        SceTouchData t{};
+        if (sceTouchPeek(SCE_TOUCH_PORT_BACK, &t, 1) > 0) {
+            for (int i = 0; i < t.reportNum; i++) {
+                if (t.report[i].y < (panelInfoBack.minAaY + panelInfoBack.maxAaY) / 2) {
+                    if (t.report[i].x < (panelInfoBack.minAaX + panelInfoBack.maxAaX) / 2) {
+                        if (t.report[i].x >= panelInfoBack.minAaX + BACK_TOUCH_MARGIN) rear_buttons |= SCE_CTRL_L2;
+                    } else {
+                        if (t.report[i].x < panelInfoBack.maxAaX - BACK_TOUCH_MARGIN) rear_buttons |= SCE_CTRL_R2;
+                    }
                 }
@@ -333,2 +360,4 @@ void pollPad() {
     }
+    old_buttons = current_buttons;
+    current_buttons = fndk_translate_pad_buttons(pad.buttons, rear_buttons, handheld);
 
@@ -340,3 +369,6 @@ void pollPad() {
         if (pressed_buttons & m.sce_button) {
-            inputEvent e;
+            inputEvent e{};
+            e.device_id = FNDK_GAMEPAD_DEVICE_ID;
+            e.repeatcount = 0;
+            e.scancode = 0;
             e.source = fndk_button_event_source; // Override fndk_button_event_source to change, e.g. to AINPUT_SOURCE_DPAD
@@ -346,2 +378,5 @@ void pollPad() {
 
+#ifdef ZOMBIE_DEBUG_BUILD
+            ALOGD("[INPUT] button deviceId=%d source=0x%x keycode=%d DOWN repeatcount=%d", e.device_id, e.source, e.keycode, e.repeatcount);
+#endif
             AInputEvent* aie = AInputEvent_create(&e);
@@ -349,3 +384,6 @@ void pollPad() {
         } else if (released_buttons & m.sce_button) {
-            inputEvent e;
+            inputEvent e{};
+            e.device_id = FNDK_GAMEPAD_DEVICE_ID;
+            e.repeatcount = 0;
+            e.scancode = 0;
             e.source = fndk_button_event_source;
@@ -355,2 +393,5 @@ void pollPad() {
 
+#ifdef ZOMBIE_DEBUG_BUILD
+            ALOGD("[INPUT] button deviceId=%d source=0x%x keycode=%d UP repeatcount=%d", e.device_id, e.source, e.keycode, e.repeatcount);
+#endif
             AInputEvent *aie = AInputEvent_create(&e);
@@ -373,3 +414,10 @@ void pollPad() {
 
-    sendJoyEvent(lx, ly, rx, ry, 0, 0, current_buttons & SCE_CTRL_L2, current_buttons & SCE_CTRL_R2);
+    float hat_x = float(bool(current_buttons & SCE_CTRL_RIGHT)) - float(bool(current_buttons & SCE_CTRL_LEFT));
+    float hat_y = float(bool(current_buttons & SCE_CTRL_DOWN)) - float(bool(current_buttons & SCE_CTRL_UP));
+    float lt = handheld ? float(bool(current_buttons & SCE_CTRL_L2)) : pad.lt / 255.0f;
+    float rt = handheld ? float(bool(current_buttons & SCE_CTRL_R2)) : pad.rt / 255.0f;
+    // Controllers without pressure bytes still provide digital triggers.
+    if (!handheld && lt == 0 && (current_buttons & SCE_CTRL_L2)) lt = 1;
+    if (!handheld && rt == 0 && (current_buttons & SCE_CTRL_R2)) rt = 1;
+    sendJoyEvent(lx, ly, rx, ry, hat_x, hat_y, lt, rt);
 }
diff --git a/shim/fndk_controls.h b/shim/fndk_controls.h
index 7173262..4e9800e 100644
--- a/shim/fndk_controls.h
+++ b/shim/fndk_controls.h
@@ -38,2 +38,5 @@ extern int fndk_button_event_source;
 
+/* Translate physical controls once before producing both keycodes and axes.
+ * Rear bits use SCE_CTRL_L2/R2. External controllers must stay standard. */
+uint32_t fndk_translate_pad_buttons(uint32_t buttons, uint32_t rear, bool handheld);
 void fndk_controls_init(AInputQueue * queue);
